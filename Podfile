# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'swiftui-omdb' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for swiftui-omdb
  pod 'Moya', '14.0.0'
  pod 'Kingfisher'
  pod 'RealmSwift'

  target 'swiftui-omdbTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'swiftui-omdbUITests' do
    # Pods for testing
  end

end
